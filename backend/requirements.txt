fastapi>=0.110.0          # Web framework (Mature and stable version)
uvicorn[standard]>=0.29.0 # ASGI server (Compatible with Python 3.12+)
psycopg2-binary>=2.9.9    # PostgreSQL driver (Version 2.9.9+ officially supports Py3.12)
python-multipart>=0.0.9   # Required for uploads (UploadFile)
requests>=2.31.0          # HTTP requests (Recent secure version)
python-dotenv>=1.0.1      # Environment variables management
pydantic>=2.6.0           # Data validation (Version 2.x for FastAPI compatibility)
